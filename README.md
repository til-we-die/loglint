# Loglint — статический анализатор лог-сообщений для Go

## Описание

`loglint` — кастомный статический анализатор на базе `golang.org/x/tools/go/analysis`, который проверяет лог-сообщения (`slog`, `zap` и совместимые вызовы) на соответствие следующим правилам:

* сообщение начинается со строчной буквы
* сообщение на английском (латиница)
* отсутствие эмодзи и спецсимволов
* отсутствие чувствительных данных (password, token, secret и т.д.)

Анализ выполняется на уровне AST, без выполнения программы.

---

# Требования

* Go ≥ 1.21 (рекомендуется 1.24+)
* Git
* Любая Unix-подобная система

Проверить версию Go:

```bash
go version
```

---

# Установка

## 1. Клонирование репозитория

```bash
git clone https://github.com/til-we-die/loglint.git
cd loglint
```

## 2. Установка зависимостей

```bash
go mod tidy
```


---

# Какие логгеры поддерживаются

* `log/slog`
* `zap.Logger`
* любые вызовы вида:

```go
logger.Info("message")
logger.Error("message")
```

---

# Примеры проверяемых нарушений

## 1. Заглавная буква в начале

```go
slog.Info("Server started")
```

## 2. Чувствительные данные

```go
slog.Info("user password: 123")
```

## 3. Множественная пунктуация

```go
slog.Info("failed!!")
slog.Info("warning...")
```

## 4. Эмодзи и спецсимволы

```go
slog.Info("server started 😊")
```

---
